import React from "react";
import styles from "../css/footer.module.css";
const Footer = (props) => {
  return <section className={styles.footer}>dream your code</section>;
};

export default Footer;
